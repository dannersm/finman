{
  "name": "finman-root",
  "version": "1.0.0",
  "private": true,
  "description": "Financial Manager with automated bank scraping",
  "workspaces": [
    "frontend",
    "scraper"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=finman-frontend & npm start --workspace=finman-scraper & wait",
    "lint": "npm run lint --workspaces --if-present"
  },
  "devDependencies": {
    "prettier": "^3.2.5"
  },
  "overrides": {
    "date-fns": "^3.6.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
